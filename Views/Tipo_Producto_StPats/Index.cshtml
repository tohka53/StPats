@model IEnumerable<StPats.Models.Tipo_Producto_StPats>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.descripcion_producto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.descripcion_producto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tipo_producto }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tipo_producto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tipo_producto })
        </td>
    </tr>
}

</table>
